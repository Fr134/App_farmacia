export default function DashboardPage() {
  return (
    <div>
      <h1 className="text-xl font-semibold text-text-primary">Dashboard</h1>
      <p className="mt-2 text-sm text-text-muted">
        Carica un report CSV per visualizzare i dati.
      </p>
    </div>
  );
}
